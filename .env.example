# Variáveis utilizadas no front-end (expostas via Vite)
VITE_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
VITE_SUPABASE_ANON_KEY=""
# Gere com `npm run gen:vapid`
VITE_VAPID_PUBLIC_KEY=""
VITE_DEFAULT_TIMEZONE="America/Sao_Paulo"

# Chaves privadas devem ser armazenadas fora do bundle (Supabase Edge Functions / secrets)
# Mantidas aqui apenas como referência para geração local.
SUPABASE_SERVICE_ROLE_KEY=""
# Resultado do `npm run gen:vapid`
VAPID_PRIVATE_KEY=""
